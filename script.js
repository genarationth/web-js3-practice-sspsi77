const addNumbers = function (a,b) {
    return a + b;
}
console.log(addNumbers(6, 2));

const subtractNumbers = function (a,b) {
    return a - b;
}
console.log(subtractNumbers(6, 2));

const multiplyNumber = function (a, b) {
    return a * b;
}
console.log(multiplyNumber(6, 2));

const divideNumber = function (a, b) {
    return a / b;
}
console.log(divideNumber(6, 2));

const createText = function (firstName, location, hobby) {
    console.log("Hi, my name is " + firstName + " I live in " + location + " and enjoy "+hobby);
}

createText('Sasipatr', 'Thailand', 'listening to music.');
